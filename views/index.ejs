<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <title>我的网页集合</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-gray-50 to-gray-100 min-h-screen">
  <div class="max-w-6xl mx-auto py-12 px-6">
    <header class="flex items-center justify-between mb-10">
      <h1 class="text-4xl font-extrabold text-gray-800">🌐 我的网页集合</h1>
      <a href="/new" class="px-5 py-2.5 bg-blue-600 text-white rounded-xl shadow hover:bg-blue-700 transition">
        ➕ 新建页面
      </a>
    </header>

    <% if (pages.length === 0) { %>
      <div class="text-center text-gray-500 mt-20">
        还没有页面，点击右上角 <span class="font-semibold">新建页面</span> 开始吧！
      </div>
    <% } else { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
        <% pages.forEach(page => { %>
          <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:shadow-2xl transition">
            <a href="/<%= page.slug %>" target="_blank">
              <div class="h-40 bg-gray-200 flex items-center justify-center overflow-hidden">
                <% if (page.thumbnail) { %>
                  <img src="<%= page.thumbnail %>" class="w-full h-full object-cover" />
                <% } else { %>
                  <span class="text-gray-400">无缩略图</span>
                <% } %>
              </div>
              <div class="p-5">
                <h2 class="text-lg font-semibold text-gray-800 truncate"><%= page.name %></h2>
                <p class="text-sm text-gray-500 mt-2">创建时间: <%= new Date(page.time).toLocaleString() %></p>
              </div>
            </a>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>
</body>
</html>
